pluginManagement {
    repositories {
        google()
        maven { url 'https://maven.aliyun.com/repository/central' }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url 'https://maven.aliyun.com/repository/central' }
    }
}

rootProject.name = "VoxLink"
include ':app'
